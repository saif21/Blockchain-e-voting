{% extends 'layout.html' %}

{% block body %}
{% from "includes/_message.html" import render_field %}
  <h1>Add Nodes</h1>
  <form action="" method="POST">
    <div class="form-group">
      <label>Enter Node Address</label>
      <input type="text" name="nodes" class="form-control" value={{request.form.nodes}}>
    </div>
    <button type="submit" class="btn btn-primary">Connect</button>
  </form>
  {% block content %}
    {{response}}
  {% endblock %}
{% endblock %}
